import { useCallback,useEffect,useState } from "react";import { useSesion } from "./useSesion";import { ErrorApi } from "../types/api.types";import type { RespuestaLista } from "../types/produccion.types";
export const IntLimiteProduccion=20;
export function Produccion_mensajeError(ObjError:unknown){if(!(ObjError instanceof ErrorApi))return"No fue posible completar la operación.";const M:Record<string,string>={VALIDACION_INVALIDA:"Revise los datos ingresados.",PERMISO_INSUFICIENTE:"No tiene permiso para esta operación.",LOTE_NO_ENCONTRADO:"El lote ya no existe.",LOTE_CERRADO:"El lote está cerrado.",LOTE_NO_ESTA_VACIO:"No puede cerrar un lote con animales vigentes.",TIPO_LOTE_INCOMPATIBLE:"El animal y el lote deben pertenecer al mismo tipo.",ANIMAL_NO_ENCONTRADO:"El animal ya no existe.",ANIMAL_NO_ACTIVO:"El animal no está activo.",ANIMAL_NO_PERTENECE_LOTE:"El animal no pertenece al lote origen.",TRASLADO_INVALIDO:"Seleccione lotes de origen y destino diferentes.",MADRE_INVALIDA:"La madre seleccionada no es compatible.",RAZA_INACTIVA:"La raza está inactiva.",RAZA_NO_CORRESPONDE_TIPO:"La raza no corresponde al tipo animal.",PROVEEDOR_INACTIVO:"El proveedor está inactivo.",OPERACION_YA_REVERTIDA:"La operación ya fue revertida.",REVERSION_NO_PERMITIDA:"Esta operación no admite reversión.",REGISTRO_DUPLICADO:"Ya existe un registro con esos datos."};return M[ObjError.StrCodigo]??ObjError.message}
export function useProduccionLista<T, F extends object>(Produccion_solicitar:(Obj:F&{pagina:number;limite:number})=>Promise<RespuestaLista<T>>,ObjInicial:F){const{Autenticacion_manejarErrorProtegido}=useSesion();const[ArrDatos,setDatos]=useState<T[]>([]),[IntTotal,setTotal]=useState(0),[IntPagina,setPagina]=useState(1),[ObjFiltros,setFiltros]=useState(ObjInicial),[BoolCargando,setCargando]=useState(true),[StrError,setError]=useState<string|null>(null);const Produccion_cargar=useCallback(async()=>{setError(null);try{const R=await Produccion_solicitar({...ObjFiltros,pagina:IntPagina,limite:IntLimiteProduccion});setDatos(R.datos);setTotal(R.paginacion.total)}catch(E){if(!Autenticacion_manejarErrorProtegido(E))setError(Produccion_mensajeError(E))}finally{setCargando(false)}},[Produccion_solicitar,ObjFiltros,IntPagina,Autenticacion_manejarErrorProtegido]);useEffect(()=>{void Produccion_cargar()},[Produccion_cargar]);return{ArrDatos,IntTotal,IntPagina,BoolCargando,StrError,establecerPagina:setPagina,Produccion_aplicarFiltros:(F:F)=>{setPagina(1);setFiltros(F)},Produccion_recargar:Produccion_cargar}}
