generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "sqlserver"
}

// ======================================================
// MÓDULO: USUARIOS
// ======================================================

model UsuarioRol {
  rolId              Int      @id @default(autoincrement()) @map("rol_id")
  nombre             String   @unique @db.NVarChar(100)
  descripcion        String?  @db.NVarChar(500)
  activo             Boolean  @default(true)
  fechaCreacion      DateTime @default(now()) @map("fecha_creacion") @db.DateTime2
  fechaActualizacion DateTime @updatedAt @map("fecha_actualizacion") @db.DateTime2

  usuarios      UsuarioCuenta[]
  rolesPermisos UsuarioRolPermiso[]

  @@map("usuarios_roles")
}

model UsuarioPermiso {
  permisoId     Int      @id @default(autoincrement()) @map("permiso_id")
  codigo        String   @unique @db.NVarChar(100)
  nombre        String   @db.NVarChar(150)
  modulo        String   @db.NVarChar(100)
  accion        String   @db.NVarChar(100)
  activo        Boolean  @default(true)
  fechaCreacion DateTime @default(now()) @map("fecha_creacion") @db.DateTime2

  rolesPermisos UsuarioRolPermiso[]

  @@map("usuarios_permisos")
}

model UsuarioCuenta {
  usuarioId          Int      @id @default(autoincrement()) @map("usuario_id")
  rolId              Int      @map("rol_id")
  nombreCompleto     String   @map("nombre_completo") @db.NVarChar(200)
  nombreUsuario      String   @unique @map("nombre_usuario") @db.NVarChar(100)
  correo             String   @unique @db.NVarChar(200)
  contrasenaHash     String   @map("contrasena_hash") @db.NVarChar(255)
  estado             String   @default("ACTIVO") @db.NVarChar(20)
  fechaCreacion      DateTime @default(now()) @map("fecha_creacion") @db.DateTime2
  fechaActualizacion DateTime @updatedAt @map("fecha_actualizacion") @db.DateTime2

  rol UsuarioRol @relation(fields: [rolId], references: [rolId], onDelete: NoAction, onUpdate: NoAction)

  sesiones  UsuarioSesion[]
  bitacoras UsuarioBitacora[]

  permisosAsignados          UsuarioRolPermiso[]         @relation("UsuarioAsignadorRolPermiso")
  asignacionesProduccion     ProduccionAsignacionLote[]
  historialEstadosProduccion ProduccionHistorialEstado[]
  medicionesProduccion       ProduccionMedicion[]
  alimentacionAsignaciones   AlimentacionAsignacion[]
  alimentacionRegistros      AlimentacionRegistro[]
  sanidadAplicaciones        SanidadAplicacion[]

  @@index([rolId])
  @@map("usuarios_cuentas")
}

model UsuarioRolPermiso {
  rolPermisoId       Int      @id @default(autoincrement()) @map("rol_permiso_id")
  rolId              Int      @map("rol_id")
  permisoId          Int      @map("permiso_id")
  usuarioAsignadorId Int      @map("usuario_asignador_id")
  fechaAsignacion    DateTime @default(now()) @map("fecha_asignacion") @db.DateTime2

  rol UsuarioRol @relation(fields: [rolId], references: [rolId], onDelete: NoAction, onUpdate: NoAction)

  permiso UsuarioPermiso @relation(fields: [permisoId], references: [permisoId], onDelete: NoAction, onUpdate: NoAction)

  usuarioAsignador UsuarioCuenta @relation("UsuarioAsignadorRolPermiso", fields: [usuarioAsignadorId], references: [usuarioId], onDelete: NoAction, onUpdate: NoAction)

  @@unique([rolId, permisoId])
  @@index([rolId])
  @@index([permisoId])
  @@index([usuarioAsignadorId])
  @@map("usuarios_roles_permisos")
}

model UsuarioSesion {
  sesionId        Int       @id @default(autoincrement()) @map("sesion_id")
  usuarioId       Int       @map("usuario_id")
  token           String    @unique @db.NVarChar(500)
  fechaInicio     DateTime  @default(now()) @map("fecha_inicio") @db.DateTime2
  fechaExpiracion DateTime  @map("fecha_expiracion") @db.DateTime2
  fechaCierre     DateTime? @map("fecha_cierre") @db.DateTime2
  estado          String    @default("ACTIVA") @db.NVarChar(20)
  direccionIp     String?   @map("direccion_ip") @db.NVarChar(45)

  usuario UsuarioCuenta @relation(fields: [usuarioId], references: [usuarioId], onDelete: NoAction, onUpdate: NoAction)

  @@index([usuarioId])
  @@index([estado])
  @@map("usuarios_sesiones")
}

model UsuarioBitacora {
  bitacoraId  Int      @id @default(autoincrement()) @map("bitacora_id")
  usuarioId   Int?     @map("usuario_id")
  modulo      String   @db.NVarChar(100)
  accion      String   @db.NVarChar(100)
  descripcion String   @db.NVarChar(1000)
  resultado   String   @db.NVarChar(50)
  fecha       DateTime @default(now()) @db.DateTime2
  direccionIp String?  @map("direccion_ip") @db.NVarChar(45)

  usuario UsuarioCuenta? @relation(fields: [usuarioId], references: [usuarioId], onDelete: NoAction, onUpdate: NoAction)

  @@index([usuarioId])
  @@index([modulo])
  @@index([fecha])
  @@map("usuarios_bitacora")
}

// ======================================================
// MÓDULO: CLIENTES Y PROVEEDORES
// ======================================================

model ClienteRegistro {
  clienteId          Int      @id @default(autoincrement()) @map("cliente_id")
  nombreCompleto     String   @map("nombre_completo") @db.NVarChar(200)
  numeroDocumento    String?  @map("numero_documento") @db.NVarChar(50)
  nit                String?  @db.NVarChar(20)
  telefono           String?  @db.NVarChar(30)
  correo             String?  @db.NVarChar(200)
  direccion          String?  @db.NVarChar(500)
  tipoCliente        String?  @map("tipo_cliente") @db.NVarChar(50)
  observaciones      String?  @db.NVarChar(1000)
  activo             Boolean  @default(true)
  fechaCreacion      DateTime @default(now()) @map("fecha_creacion") @db.DateTime2
  fechaActualizacion DateTime @updatedAt @map("fecha_actualizacion") @db.DateTime2

  @@index([nombreCompleto])
  @@index([nit])
  @@index([activo])
  @@map("clientes_registros")
}

model ProveedorRegistro {
  proveedorId        Int      @id @default(autoincrement()) @map("proveedor_id")
  nombre             String   @db.NVarChar(200)
  nombreComercial    String?  @map("nombre_comercial") @db.NVarChar(200)
  numeroDocumento    String?  @map("numero_documento") @db.NVarChar(50)
  nit                String?  @db.NVarChar(20)
  telefono           String?  @db.NVarChar(30)
  correo             String?  @db.NVarChar(200)
  direccion          String?  @db.NVarChar(500)
  tipoProveedor      String?  @map("tipo_proveedor") @db.NVarChar(50)
  observaciones      String?  @db.NVarChar(1000)
  activo             Boolean  @default(true)
  fechaCreacion      DateTime @default(now()) @map("fecha_creacion") @db.DateTime2
  fechaActualizacion DateTime @updatedAt @map("fecha_actualizacion") @db.DateTime2

  productos       ProveedorProducto[]
  lotesInventario InventarioLote[]

  @@index([nombre])
  @@index([nit])
  @@index([activo])
  @@map("proveedores_registros")
}

// ======================================================
// MÓDULO: INVENTARIO
// ======================================================

model InventarioCategoria {
  categoriaId        Int      @id @default(autoincrement()) @map("categoria_id")
  nombre             String   @unique @db.NVarChar(150)
  descripcion        String?  @db.NVarChar(500)
  activo             Boolean  @default(true)
  fechaCreacion      DateTime @default(now()) @map("fecha_creacion") @db.DateTime2
  fechaActualizacion DateTime @updatedAt @map("fecha_actualizacion") @db.DateTime2

  productos InventarioProducto[]

  @@index([activo])
  @@map("inventario_categorias")
}

model InventarioProducto {
  productoId         Int      @id @default(autoincrement()) @map("producto_id")
  categoriaId        Int      @map("categoria_id")
  codigo             String   @unique @db.NVarChar(50)
  nombre             String   @db.NVarChar(200)
  descripcion        String?  @db.NVarChar(500)
  unidadMedida       String   @map("unidad_medida") @db.NVarChar(50)
  manejaLotes        Boolean  @default(false) @map("maneja_lotes")
  activo             Boolean  @default(true)
  fechaCreacion      DateTime @default(now()) @map("fecha_creacion") @db.DateTime2
  fechaActualizacion DateTime @updatedAt @map("fecha_actualizacion") @db.DateTime2

  categoria InventarioCategoria @relation(fields: [categoriaId], references: [categoriaId], onDelete: NoAction, onUpdate: NoAction)

  proveedores          ProveedorProducto[]
  existencias          InventarioExistencia[]
  formulaDetalles      AlimentacionFormulaDetalle[]
  alimentacionDetalles AlimentacionDetalle[]
  sanidadAplicaciones  SanidadAplicacion[]

  @@index([categoriaId])
  @@index([nombre])
  @@index([activo])
  @@map("inventario_productos")
}

model InventarioAlmacen {
  inventarioId       Int      @id @default(autoincrement()) @map("inventario_id")
  nombre             String   @db.NVarChar(150)
  descripcion        String?  @db.NVarChar(500)
  ubicacion          String?  @db.NVarChar(300)
  activo             Boolean  @default(true)
  fechaCreacion      DateTime @default(now()) @map("fecha_creacion") @db.DateTime2
  fechaActualizacion DateTime @updatedAt @map("fecha_actualizacion") @db.DateTime2

  existencias InventarioExistencia[]

  @@index([activo])
  @@map("inventario_almacenes")
}

model InventarioExistencia {
  inventarioProductoId Int      @id @default(autoincrement()) @map("inventario_producto_id")
  inventarioId         Int      @map("inventario_id")
  productoId           Int      @map("producto_id")
  existenciaActual     Decimal  @default(0) @map("existencia_actual") @db.Decimal(18, 4)
  existenciaMinima     Decimal  @default(0) @map("existencia_minima") @db.Decimal(18, 4)
  activo               Boolean  @default(true)
  fechaCreacion        DateTime @default(now()) @map("fecha_creacion") @db.DateTime2
  fechaActualizacion   DateTime @updatedAt @map("fecha_actualizacion") @db.DateTime2

  almacen  InventarioAlmacen  @relation(fields: [inventarioId], references: [inventarioId], onDelete: NoAction, onUpdate: NoAction)
  producto InventarioProducto @relation(fields: [productoId], references: [productoId], onDelete: NoAction, onUpdate: NoAction)

  lotes InventarioLote[]

  @@unique([inventarioId, productoId])
  @@index([inventarioId])
  @@index([productoId])
  @@index([activo])
  @@map("inventario_existencias")
}

model InventarioLote {
  loteInventarioId     Int       @id @default(autoincrement()) @map("lote_inventario_id")
  inventarioProductoId Int       @map("inventario_producto_id")
  proveedorId          Int?      @map("proveedor_id")
  codigoLote           String    @map("codigo_lote") @db.NVarChar(100)
  fechaIngreso         DateTime  @default(now()) @map("fecha_ingreso") @db.DateTime2
  fechaFabricacion     DateTime? @map("fecha_fabricacion") @db.Date
  fechaVencimiento     DateTime? @map("fecha_vencimiento") @db.Date
  costoUnitario        Decimal?  @map("costo_unitario") @db.Decimal(18, 4)
  existenciaActual     Decimal   @default(0) @map("existencia_actual") @db.Decimal(18, 4)
  activo               Boolean   @default(true)
  observaciones        String?   @db.NVarChar(1000)
  fechaCreacion        DateTime  @default(now()) @map("fecha_creacion") @db.DateTime2
  fechaActualizacion   DateTime  @updatedAt @map("fecha_actualizacion") @db.DateTime2

  existencia InventarioExistencia @relation(fields: [inventarioProductoId], references: [inventarioProductoId], onDelete: NoAction, onUpdate: NoAction)

  proveedor ProveedorRegistro? @relation(fields: [proveedorId], references: [proveedorId], onDelete: NoAction, onUpdate: NoAction)

  @@index([inventarioProductoId])
  @@index([proveedorId])
  @@index([codigoLote])
  @@index([fechaVencimiento])
  @@index([activo])
  @@map("inventario_lotes")
}

model ProveedorProducto {
  proveedorProductoId Int      @id @default(autoincrement()) @map("proveedor_producto_id")
  proveedorId         Int      @map("proveedor_id")
  productoId          Int      @map("producto_id")
  precioReferencia    Decimal? @map("precio_referencia") @db.Decimal(18, 4)
  activo              Boolean  @default(true)
  fechaRelacion       DateTime @default(now()) @map("fecha_relacion") @db.DateTime2
  fechaActualizacion  DateTime @updatedAt @map("fecha_actualizacion") @db.DateTime2

  proveedor ProveedorRegistro @relation(fields: [proveedorId], references: [proveedorId], onDelete: NoAction, onUpdate: NoAction)

  producto InventarioProducto @relation(fields: [productoId], references: [productoId], onDelete: NoAction, onUpdate: NoAction)

  @@unique([proveedorId, productoId])
  @@index([proveedorId])
  @@index([productoId])
  @@index([activo])
  @@map("proveedores_productos")
}

// ======================================================
// MÓDULO: PRODUCCIÓN
// ======================================================

model ProduccionTipoAnimal {
  tipoAnimalId       Int      @id @default(autoincrement()) @map("tipo_animal_id")
  nombre             String   @unique @db.NVarChar(100)
  descripcion        String?  @db.NVarChar(500)
  activo             Boolean  @default(true)
  fechaCreacion      DateTime @default(now()) @map("fecha_creacion") @db.DateTime2
  fechaActualizacion DateTime @updatedAt @map("fecha_actualizacion") @db.DateTime2

  razas    ProduccionRaza[]
  animales ProduccionAnimal[]

  @@index([activo])
  @@map("produccion_tipos_animales")
}

model ProduccionRaza {
  razaId             Int      @id @default(autoincrement()) @map("raza_id")
  tipoAnimalId       Int      @map("tipo_animal_id")
  nombre             String   @db.NVarChar(150)
  descripcion        String?  @db.NVarChar(500)
  activo             Boolean  @default(true)
  fechaCreacion      DateTime @default(now()) @map("fecha_creacion") @db.DateTime2
  fechaActualizacion DateTime @updatedAt @map("fecha_actualizacion") @db.DateTime2

  tipoAnimal ProduccionTipoAnimal @relation(fields: [tipoAnimalId], references: [tipoAnimalId], onDelete: NoAction, onUpdate: NoAction)
  animales   ProduccionAnimal[]

  @@unique([tipoAnimalId, nombre])
  @@index([tipoAnimalId])
  @@index([activo])
  @@map("produccion_razas")
}

model ProduccionLote {
  loteProduccionId   Int       @id @default(autoincrement()) @map("lote_produccion_id")
  codigo             String    @unique @db.NVarChar(50)
  nombre             String    @db.NVarChar(150)
  descripcion        String?   @db.NVarChar(500)
  fechaInicio        DateTime  @default(now()) @map("fecha_inicio") @db.DateTime2
  fechaCierre        DateTime? @map("fecha_cierre") @db.DateTime2
  estado             String    @default("ACTIVO") @db.NVarChar(20)
  fechaCreacion      DateTime  @default(now()) @map("fecha_creacion") @db.DateTime2
  fechaActualizacion DateTime  @updatedAt @map("fecha_actualizacion") @db.DateTime2

  asignaciones             ProduccionAsignacionLote[]
  mediciones               ProduccionMedicion[]
  alimentacionAsignaciones AlimentacionAsignacion[]
  alimentacionRegistros    AlimentacionRegistro[]
  sanidadAplicaciones      SanidadAplicacion[]

  @@index([estado])
  @@index([fechaInicio])
  @@map("produccion_lotes")
}

model ProduccionAnimal {
  animalId           Int       @id @default(autoincrement()) @map("animal_id")
  tipoAnimalId       Int       @map("tipo_animal_id")
  razaId             Int?      @map("raza_id")
  identificacion     String    @unique @db.NVarChar(100)
  sexo               String    @db.NVarChar(20)
  fechaNacimiento    DateTime? @map("fecha_nacimiento") @db.Date
  fechaIngreso       DateTime  @default(now()) @map("fecha_ingreso") @db.DateTime2
  estadoActual       String    @default("ACTIVO") @map("estado_actual") @db.NVarChar(20)
  observaciones      String?   @db.NVarChar(1000)
  fechaCreacion      DateTime  @default(now()) @map("fecha_creacion") @db.DateTime2
  fechaActualizacion DateTime  @updatedAt @map("fecha_actualizacion") @db.DateTime2

  tipoAnimal               ProduccionTipoAnimal        @relation(fields: [tipoAnimalId], references: [tipoAnimalId], onDelete: NoAction, onUpdate: NoAction)
  raza                     ProduccionRaza?             @relation(fields: [razaId], references: [razaId], onDelete: NoAction, onUpdate: NoAction)
  asignaciones             ProduccionAsignacionLote[]
  historialEstados         ProduccionHistorialEstado[]
  mediciones               ProduccionMedicion[]
  alimentacionAsignaciones AlimentacionAsignacion[]
  alimentacionRegistros    AlimentacionRegistro[]
  sanidadAplicaciones      SanidadAplicacion[]

  @@index([tipoAnimalId])
  @@index([razaId])
  @@index([estadoActual])
  @@map("produccion_animales")
}

model ProduccionAsignacionLote {
  asignacionLoteId Int       @id @default(autoincrement()) @map("asignacion_lote_id")
  animalId         Int       @map("animal_id")
  loteProduccionId Int       @map("lote_produccion_id")
  usuarioId        Int       @map("usuario_id")
  fechaInicio      DateTime  @default(now()) @map("fecha_inicio") @db.DateTime2
  fechaFin         DateTime? @map("fecha_fin") @db.DateTime2
  motivoCambio     String?   @map("motivo_cambio") @db.NVarChar(500)
  estado           String    @default("VIGENTE") @db.NVarChar(20)

  animal  ProduccionAnimal @relation(fields: [animalId], references: [animalId], onDelete: NoAction, onUpdate: NoAction)
  lote    ProduccionLote   @relation(fields: [loteProduccionId], references: [loteProduccionId], onDelete: NoAction, onUpdate: NoAction)
  usuario UsuarioCuenta    @relation(fields: [usuarioId], references: [usuarioId], onDelete: NoAction, onUpdate: NoAction)

  @@index([animalId])
  @@index([loteProduccionId])
  @@index([usuarioId])
  @@index([estado])
  @@map("produccion_asignaciones_lotes")
}

model ProduccionHistorialEstado {
  historialEstadoId Int      @id @default(autoincrement()) @map("historial_estado_id")
  animalId          Int      @map("animal_id")
  usuarioId         Int      @map("usuario_id")
  estadoAnterior    String?  @map("estado_anterior") @db.NVarChar(30)
  estadoNuevo       String   @map("estado_nuevo") @db.NVarChar(30)
  motivo            String?  @db.NVarChar(500)
  fechaCambio       DateTime @default(now()) @map("fecha_cambio") @db.DateTime2

  animal  ProduccionAnimal @relation(fields: [animalId], references: [animalId], onDelete: NoAction, onUpdate: NoAction)
  usuario UsuarioCuenta    @relation(fields: [usuarioId], references: [usuarioId], onDelete: NoAction, onUpdate: NoAction)

  @@index([animalId])
  @@index([usuarioId])
  @@index([fechaCambio])
  @@map("produccion_historial_estados")
}

model ProduccionMedicion {
  medicionId       Int      @id @default(autoincrement()) @map("medicion_id")
  animalId         Int?     @map("animal_id")
  loteProduccionId Int?     @map("lote_produccion_id")
  usuarioId        Int      @map("usuario_id")
  tipoMedicion     String   @map("tipo_medicion") @db.NVarChar(50)
  valor            Decimal  @db.Decimal(18, 4)
  unidadMedida     String   @map("unidad_medida") @db.NVarChar(30)
  fechaMedicion    DateTime @default(now()) @map("fecha_medicion") @db.DateTime2
  observaciones    String?  @db.NVarChar(1000)

  animal  ProduccionAnimal? @relation(fields: [animalId], references: [animalId], onDelete: NoAction, onUpdate: NoAction)
  lote    ProduccionLote?   @relation(fields: [loteProduccionId], references: [loteProduccionId], onDelete: NoAction, onUpdate: NoAction)
  usuario UsuarioCuenta     @relation(fields: [usuarioId], references: [usuarioId], onDelete: NoAction, onUpdate: NoAction)

  @@index([animalId])
  @@index([loteProduccionId])
  @@index([usuarioId])
  @@index([fechaMedicion])
  @@map("produccion_mediciones")
}

// ======================================================
// MÓDULO: ALIMENTACIÓN
// ======================================================

model AlimentacionFormula {
  formulaId          Int      @id @default(autoincrement()) @map("formula_id")
  nombre             String   @unique @db.NVarChar(150)
  descripcion        String?  @db.NVarChar(500)
  cantidadBase       Decimal  @map("cantidad_base") @db.Decimal(18, 4)
  unidadBase         String   @map("unidad_base") @db.NVarChar(30)
  activo             Boolean  @default(true)
  fechaCreacion      DateTime @default(now()) @map("fecha_creacion") @db.DateTime2
  fechaActualizacion DateTime @updatedAt @map("fecha_actualizacion") @db.DateTime2

  detalles     AlimentacionFormulaDetalle[]
  asignaciones AlimentacionAsignacion[]
  registros    AlimentacionRegistro[]

  @@index([activo])
  @@map("alimentacion_formulas")
}

model AlimentacionFormulaDetalle {
  detalleFormulaId   Int      @id @default(autoincrement()) @map("detalle_formula_id")
  formulaId          Int      @map("formula_id")
  productoId         Int      @map("producto_id")
  cantidad           Decimal  @db.Decimal(18, 4)
  unidadMedida       String   @map("unidad_medida") @db.NVarChar(30)
  activo             Boolean  @default(true)
  fechaCreacion      DateTime @default(now()) @map("fecha_creacion") @db.DateTime2
  fechaActualizacion DateTime @updatedAt @map("fecha_actualizacion") @db.DateTime2

  formula  AlimentacionFormula @relation(fields: [formulaId], references: [formulaId], onDelete: NoAction, onUpdate: NoAction)
  producto InventarioProducto  @relation(fields: [productoId], references: [productoId], onDelete: NoAction, onUpdate: NoAction)

  @@unique([formulaId, productoId])
  @@index([formulaId])
  @@index([productoId])
  @@index([activo])
  @@map("alimentacion_formulas_detalles")
}

model AlimentacionAsignacion {
  asignacionFormulaId Int       @id @default(autoincrement()) @map("asignacion_formula_id")
  formulaId           Int       @map("formula_id")
  animalId            Int?      @map("animal_id")
  loteProduccionId    Int?      @map("lote_produccion_id")
  usuarioId           Int       @map("usuario_id")
  fechaInicio         DateTime  @map("fecha_inicio") @db.Date
  fechaFin            DateTime? @map("fecha_fin") @db.Date
  estado              String    @default("VIGENTE") @db.NVarChar(20)
  observaciones       String?   @db.NVarChar(1000)

  formula AlimentacionFormula @relation(fields: [formulaId], references: [formulaId], onDelete: NoAction, onUpdate: NoAction)
  animal  ProduccionAnimal?   @relation(fields: [animalId], references: [animalId], onDelete: NoAction, onUpdate: NoAction)
  lote    ProduccionLote?     @relation(fields: [loteProduccionId], references: [loteProduccionId], onDelete: NoAction, onUpdate: NoAction)
  usuario UsuarioCuenta       @relation(fields: [usuarioId], references: [usuarioId], onDelete: NoAction, onUpdate: NoAction)

  @@index([formulaId])
  @@index([animalId])
  @@index([loteProduccionId])
  @@index([usuarioId])
  @@index([estado])
  @@map("alimentacion_asignaciones")
}

model AlimentacionRegistro {
  alimentacionId       Int      @id @default(autoincrement()) @map("alimentacion_id")
  formulaId            Int      @map("formula_id")
  animalId             Int?     @map("animal_id")
  loteProduccionId     Int?     @map("lote_produccion_id")
  usuarioId            Int      @map("usuario_id")
  fechaAlimentacion    DateTime @map("fecha_alimentacion") @db.Date
  cantidadSuministrada Decimal  @map("cantidad_suministrada") @db.Decimal(18, 4)
  unidadMedida         String   @map("unidad_medida") @db.NVarChar(30)
  observaciones        String?  @db.NVarChar(1000)
  fechaRegistro        DateTime @default(now()) @map("fecha_registro") @db.DateTime2

  formula  AlimentacionFormula   @relation(fields: [formulaId], references: [formulaId], onDelete: NoAction, onUpdate: NoAction)
  animal   ProduccionAnimal?     @relation(fields: [animalId], references: [animalId], onDelete: NoAction, onUpdate: NoAction)
  lote     ProduccionLote?       @relation(fields: [loteProduccionId], references: [loteProduccionId], onDelete: NoAction, onUpdate: NoAction)
  usuario  UsuarioCuenta         @relation(fields: [usuarioId], references: [usuarioId], onDelete: NoAction, onUpdate: NoAction)
  detalles AlimentacionDetalle[]

  @@index([formulaId])
  @@index([animalId])
  @@index([loteProduccionId])
  @@index([usuarioId])
  @@index([fechaAlimentacion])
  @@map("alimentacion_registros")
}

model AlimentacionDetalle {
  detalleAlimentacionId Int      @id @default(autoincrement()) @map("detalle_alimentacion_id")
  alimentacionId        Int      @map("alimentacion_id")
  productoId            Int      @map("producto_id")
  cantidadConsumida     Decimal  @map("cantidad_consumida") @db.Decimal(18, 4)
  unidadMedida          String   @map("unidad_medida") @db.NVarChar(30)
  fechaRegistro         DateTime @default(now()) @map("fecha_registro") @db.DateTime2

  alimentacion AlimentacionRegistro @relation(fields: [alimentacionId], references: [alimentacionId], onDelete: NoAction, onUpdate: NoAction)
  producto     InventarioProducto   @relation(fields: [productoId], references: [productoId], onDelete: NoAction, onUpdate: NoAction)

  @@unique([alimentacionId, productoId])
  @@index([alimentacionId])
  @@index([productoId])
  @@map("alimentacion_detalles")
}

// ======================================================
// MÓDULO: SANIDAD
// ======================================================

model SanidadAplicacion {
  aplicacionSanitariaId  Int       @id @default(autoincrement()) @map("aplicacion_sanitaria_id")
  animalId               Int?      @map("animal_id")
  loteProduccionId       Int?      @map("lote_produccion_id")
  productoId             Int       @map("producto_id")
  usuarioId              Int       @map("usuario_id")
  tipoAplicacion         String    @map("tipo_aplicacion") @db.NVarChar(50)
  dosis                  Decimal   @db.Decimal(18, 4)
  unidadMedida           String    @map("unidad_medida") @db.NVarChar(30)
  cantidadTotalUtilizada Decimal   @map("cantidad_total_utilizada") @db.Decimal(18, 4)
  fechaAplicacion        DateTime  @map("fecha_aplicacion") @db.Date
  proximaAplicacion      DateTime? @map("proxima_aplicacion") @db.Date
  observaciones          String?   @db.NVarChar(1000)
  fechaRegistro          DateTime  @default(now()) @map("fecha_registro") @db.DateTime2

  animal   ProduccionAnimal?  @relation(fields: [animalId], references: [animalId], onDelete: NoAction, onUpdate: NoAction)
  lote     ProduccionLote?    @relation(fields: [loteProduccionId], references: [loteProduccionId], onDelete: NoAction, onUpdate: NoAction)
  producto InventarioProducto @relation(fields: [productoId], references: [productoId], onDelete: NoAction, onUpdate: NoAction)
  usuario  UsuarioCuenta      @relation(fields: [usuarioId], references: [usuarioId], onDelete: NoAction, onUpdate: NoAction)

  @@index([animalId])
  @@index([loteProduccionId])
  @@index([productoId])
  @@index([usuarioId])
  @@index([tipoAplicacion])
  @@index([fechaAplicacion])
  @@map("sanidad_aplicaciones")
}
